<%= symphonia_form_for(@home, url: home_path, autocomplete: "off") do |f| %>
  <%= f.error_messages %>

  <fieldset>
    <legend><%= t :legend_atrea %></legend>
    <%= f.text_field :atrea_login, autocomplete: "off" %>
    <%= f.password_field :atrea_password, autocomplete: "off" %>

  </fieldset>

  <p class="buttons">
    <%= f.primary %>
  </p>
<% end %>
